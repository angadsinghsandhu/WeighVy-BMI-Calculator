// imports
import 'package:flutter/material.dart';

class WeightAge extends StatelessWidget {
  // variables
  final int weight;
  final int age;

  // constructor
  WeightAge(this.weight, this.age);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
