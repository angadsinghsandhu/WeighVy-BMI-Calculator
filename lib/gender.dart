// imports
import 'package:flutter/material.dart';

class Gender extends StatelessWidget {
  // variables
  final bool isMale;
  final bool isFemale;

  // constructor
  Gender(this.isMale, this.isFemale);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
